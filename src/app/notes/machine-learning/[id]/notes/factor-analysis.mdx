<center>
# Factor Analysis
</center>

If we have a dataset with $n$ data points and $d$ features and we want to model the data using a [mixture of gaussians](/notes/machine-learning/gaussian-mixture-models), it would be very hard to do so if $d \gg n$.

One way around this is to put restrictions on the covariance matrix of the data and force it to be diagonal where each entry is just an empirical estimate of the variance in the $j$-th coordinate.

$$
\Sigma_{jj} = \frac{1}{n} \sum_{i=1}^n \left(x_j^{(i)} - \mu_j\right)^2
$$

However, this fails to capture any correlations between the different features of our data. Therefore, a better solution is to learn a lower dimensional representation of our data.

In the factor analysis model, we assume that each feature $x_j$ is generated by a linear combination of the entries of a lower-dimensional latent variable $z$. We also assume that $z$ is normally distributed.

$$
z \sim \mathcal{N}(0, I)
$$

$$
x | z \sim \mathcal{N}(\mu + Wz, \Psi)
$$

We can rewrite this as:

$$
z \sim \mathcal{N}(0, I)
$$

$$
\epsilon \sim \mathcal{N}(0, \Psi)
$$

$$
x = \mu + Wz + \epsilon
$$

<Detour>
For any random variable that follows a normal distribution $x \sim \mathcal{N}(\mu, \sigma^2)$, it can be written in terms of $\epsilon$ where $\epsilon \sim \mathcal{N}(0, 1)$.

$$
x = \mu + \sigma \cdot \epsilon
$$
</Detour>

We can derive the mean and covariance of $x$ and see that $x$ follows the following distribution:

$$
x \sim \mathcal{N}(\mu, WW^T + \Psi)
$$

<Figure src="/images/notes/factor-analysis-covariance.png" alt="A data cloud with the diagonal covariance drawn as an axis-aligned dashed ellipse that misses the tilt, alongside the factor analysis covariance drawn as density bands tilted to follow the correlation">
    A diagonal $\Sigma$ (dashed) can only stretch along the coordinate axes, while the factor analysis covariance $WW^T + \Psi$ (blue) tilts to capture the correlation between features.
</Figure>

<Derivation>
$$
\mathbb{E}[x] = \mathbb{E}[\mu + Wz + \epsilon]
$$

$$
= \mathbb{E}[\mu + Wz + \epsilon]
$$

$$
= \mathbb{E}[\mu] + \mathbb{E}[Wz] + \mathbb{E}[\epsilon]
$$

<Info>
$$
= \mu + W \cdot \mathbb{E}[z] + \mathbb{E}[\epsilon]
$$

Expectation of a constant is the constant itself.
</Info>

<Info>
$$
= \mu
$$

Expectations of $z$ and $\epsilon$ are zero.
</Info>

$$
\text{Var}(x) = \mathbb{E} \left[(x - \mathbb{E}[x])(x - \mathbb{E}[x])^T \right]
$$

$$
= \mathbb{E}\left[(\mu + Wz + \epsilon - \mu)(\mu + Wz + \epsilon - \mu)^T\right]
$$

$$
= \mathbb{E}\left[(Wz + \epsilon)(Wz + \epsilon)^T\right]
$$

$$
= \mathbb{E}\left[Wz z^T W^T + Wz \epsilon^T + \epsilon z^T W^T + \epsilon \epsilon^T \right]
$$

$$
= W\cdot\mathbb{E}\left[z z^T \right]\cdot W^T + W \cdot \mathbb{E}\left[z \epsilon^T \right] + \mathbb{E}\left[\epsilon z^T \right]\cdot W^T + \mathbb{E}\left[\epsilon \epsilon^T \right]
$$

<Info>
$$
= W\cdot\mathbb{E}\left[z z^T \right]\cdot W^T + \mathbb{E}\left[\epsilon \epsilon^T \right]
$$

Since $\epsilon$ and $z$ are independent.

Also, $\mathbb{E}[z] = 0$ and $\mathbb{E}[\epsilon] = 0$

Thus, $\mathbb{E}[z \epsilon] = \mathbb{E}[z] \cdot \mathbb{E}[\epsilon] = 0$
</Info>

<Info>
$$
= WW^T + \Psi
$$

$$
\mathbb{E}[z z^T] = \text{Var}(z) = I
$$

$$
\mathbb{E}[\epsilon \epsilon^T] = \text{Var}(\epsilon) = \Psi
$$
</Info>
</Derivation>

Thus, we can write the log likelihood of our data under this model as:

$$
\ell(\mu, W, \Psi) = \log \prod_{i=1}^{n} \frac{1}{(2\pi)^{d/2} |W W^T + \Psi|^{1/2}} \exp \left( -\frac{1}{2} (x^{(i)} - \mu)^T (W W^T + \Psi)^{-1} (x^{(i)} - \mu) \right)
$$

However, there does not exist any closed form solution for the maximum likelihood estimate of $\mu, W, \Psi$. This is because the matrices $W$ and $\Psi$ are coupled in the likelihood function and therefore, we cannot optimize them separately.

Therefore, we need to use the [Expectation Maximization](/notes/machine-learning/em-algorithm) algorithm instead.

For the $E$-step, we need to find the estimate for our posterior distribution of $z$ given $x$. This distribution can be written as:

$$
z|x \sim \mathcal{N}\left(\mu_{z|x}, \, \Sigma_{z|x}\right)
$$

$$
\mu_{z|x} = W^T \left( WW^T + \Psi \right)^{-1} \left( x - \mu \right)
$$

$$
\Sigma_{z|x} = I - W^T \left( WW^T + \Psi \right)^{-1} W
$$

<Derivation>
<Lemma>
For any two gaussian distributions $z$ and $x$, the conditional distribution $z|x$ is also a gaussian distribution and is given by:

$$
z | x \sim \mathcal{N}\left(\mu_{z|x}, \Sigma_{z|x}\right)
$$

$$
\mu_{z|x} = \mu_z + \Sigma_{zx} \Sigma_{xx}^{-1} (x - \mu_x)
$$

$$
\Sigma_{z|x} = \Sigma_{zz} - \Sigma_{zx} \Sigma_{xx}^{-1} \Sigma_{xz}
$$
</Lemma>

From our original equations, we can see that:

$$
\mu_z = 0
$$

$$
\Sigma_{zz} = \text{Var}(z) = I
$$

Later to find the distribution of $x$, we saw that:

$$
\mu_x = \mu
$$

$$
\Sigma_{xx} = WW^T + \Psi
$$

Now,

$$
\Sigma_{zx} = \text{Cov}(z, x) = \mathbb{E} \left[(z - \mathbb{E}[z])(x - \mathbb{E}[x])^T \right]
$$

<Info>
$$
= \mathbb{E} \left[(z - 0)(x - \mu)^T \right]
$$

$\mathbb{E}[z] = 0$ and $\mathbb{E}[x] = \mu$
</Info>

<Info>
$$
= \mathbb{E} \left[z(\mu + Wz + \epsilon - \mu)^T \right]
$$

$x = \mu + Wz + \epsilon$
</Info>

$$
= \mathbb{E} \left[z z^T W^T \right] + \mathbb{E} \left[z \epsilon^T \right]
$$

<Info>
$$
= \mathbb{E} \left[z z^T \right] \cdot W^T + 0
$$

Since $z$ and $\epsilon$ are independent.

Thus, $\mathbb{E}\left[z \epsilon^T \right] = \mathbb{E}[z] \cdot \mathbb{E}[\epsilon^T] = 0$.
</Info>

<Info>
$$
= W^T
$$

$\mathbb{E}\left[z z^T \right] = \text{Var}(z) = I$
</Info>

Similarly,

$$
\Sigma_{xz} = \text{Cov}(x, z) = W
$$

Putting this all together, we get:

$$
z|x \sim \mathcal{N}\left(\mu_{z|x}, \, \Sigma_{z|x}\right)
$$

$$
\mu_{z|x} = W^T \left( WW^T + \Psi \right)^{-1} \left( x - \mu \right)
$$

$$
\Sigma_{z|x} = I - W^T \left( WW^T + \Psi \right)^{-1} W
$$
</Derivation>

Now, the $E$-step for our EM algorithm is given by:

$$
Q_i(z^{(i)}) = p\left(z^{(i)} | x^{(i)}; \mu, W, \Psi\right)
$$

$$
= \frac{1}{(2\pi)^{k/2} |\Sigma_{z^{(i)}|x^{(i)}}|^{1/2}} \exp\left( -\frac{1}{2} \left( z^{(i)} - \mu_{z^{(i)}|x^{(i)}} \right)^T \Sigma_{z^{(i)}|x^{(i)}}^{-1} \left( z^{(i)} - \mu_{z^{(i)}|x^{(i)}} \right) \right)
$$

In the $M$-step, we need to maximize our evidence lower bound:

$$
\text{ELBO}\left(x; Q, \mu, W, \Psi \right) = \sum_{i=1}^n \mathbb{E}_{z^{(i)} \sim Q_i} \left[ \log \frac{p\left(x^{(i)}, z^{(i)}; \mu, W, \Psi\right)}{Q_i(z^{(i)})} \right]
$$

Maximizing the $\text{ELBO}$ with respect to each of the parameters $\mu, W, \Psi$, we get the following update equations:

$$
\mu = \frac{1}{n} \sum_{i=1}^n x^{(i)}
$$

$$
W = \left( \sum_{i=1}^{n} (x^{(i)} - \mu) \mu_{z^{(i)}|x^{(i)}}^T \right) \left( \sum_{i=1}^{n} \mu_{z^{(i)}|x^{(i)}} \mu_{z^{(i)}|x^{(i)}}^T + \Sigma_{z^{(i)}|x^{(i)}} \right)^{-1}
$$

$$
\Psi = \frac{1}{n} \, \sum_{i=1}^{n} \left(\left(x^{(i)} - \mu\right) \left(x^{(i)} - \mu\right)^T - W \mu_{z^{(i)} \mid x^{(i)}} \left(x^{(i)} - \mu\right)^T - \left(x^{(i)} - \mu\right) \mu_{z^{(i)} \mid x^{(i)}}^T W^T \right.
$$

$$
\left. + \, W \left( \mu_{z^{(i)}|x^{(i)}} \, \mu_{z^{(i)}|x^{(i)}}^T + \Sigma_{z^{(i)}|x^{(i)}} \right) W^T \right)
$$

<Derivation className="mt-3 mb-0">
$$
\text{ELBO}\left(x; Q, \mu, W, \Psi \right) = \sum_{i=1}^n \mathbb{E}_{z^{(i)} \sim Q_i} \left[ \log \frac{p\left(x^{(i)}, z^{(i)}; \mu, W, \Psi\right)}{Q_i(z^{(i)})} \right]
$$

$$
= \sum_{i=1}^n \mathbb{E}_{z^{(i)} \sim Q_i} \left[ \log p\left(x^{(i)} \mid z^{(i)}; \mu, W, \Psi\right) + \log p\left(z^{(i)}\right) - \log Q_i\left(z^{(i)}\right) \right]
$$

<Info>
$$
\approx \sum_{i=1}^n \mathbb{E}_{z^{(i)} \sim Q_i} \left[ \log p\left(x^{(i)} \mid z^{(i)}; \mu, W, \Psi\right) \right]
$$

Dropping terms that do not depend on our parameters.

Note that in the $M$-step, $Q_i(z^{(i)})$ is just a constant.
</Info>

$$
= \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[ \log \left( \frac{1}{(2\pi)^{d/2} |\Psi|^{1/2}} \exp \left( -\frac{1}{2} (x^{(i)} - \mu - W z^{(i)})^T \Psi^{-1} (x^{(i)} - \mu - W z^{(i)}) \right) \right) \right]
$$

$$
= \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[ -\frac{1}{2} \log |\Psi| - \frac{n}{2} \log(2\pi) - \frac{1}{2} (x^{(i)} - \mu - W z^{(i)})^T \Psi^{-1} (x^{(i)} - \mu - W z^{(i)}) \right]
$$

Taking the derivative with respect to $\mu$, we get:

$$
-\frac{1}{2} \, \nabla_{\mu} \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[ \left(x^{(i)} - \mu - W z^{(i)}\right)^T \Psi^{-1} \left(x^{(i)} - \mu - W z^{(i)}\right) \right]
$$

<Info>
$$
= -\frac{1}{2} \, \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[-\nabla_{\mu} \left(\mu^T \Psi^{-1} \left(x^{(i)} - W z^{(i)}\right) \right) + \nabla_{\mu} \left(\mu^T \Psi^{-1} \mu\right) - \nabla_{\mu} \left(\left(x^{(i)} - Wz^{(i)}\right)^T \Psi^{-1} \mu \right) \right]
$$

Ignoring terms that do not depend on $\mu$.
</Info>

<Info>
$$
= -\frac{1}{2} \, \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[-\nabla_{\mu} \text{ tr} \left(\mu^T \Psi^{-1} \left(x^{(i)} - W z^{(i)}\right) \right) + \nabla_{\mu} \text{ tr} \left(\mu^T \Psi^{-1} \mu\right) - \nabla_{\mu} \text{ tr} \left(\left(x^{(i)} - Wz^{(i)}\right)^T \Psi^{-1} \mu \right) \right]
$$

All the three terms simplify to a scalar. And for a scalar $s$:

$$
\text{tr}(s) = s
$$
</Info>

<Info>
$$
= -\frac{1}{2} \, \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[-\nabla_{\mu} \text{ tr} \left(\mu^T \Psi^{-1} \left(x^{(i)} - W z^{(i)}\right) \right) + \nabla_{\mu} \text{ tr} \left(\mu^T \Psi^{-1} \mu\right) - \nabla_{\mu} \text{ tr} \left(\mu^{T} \Psi^{-T} \left(x^{(i)} - Wz^{(i)}\right) \right) \right]
$$

$$
\text{tr}(AB) = \text{tr}(BA)
$$

$$
\text{tr}(A^T) = \text{tr}(A)
$$
</Info>

<Info>
$$
= -\frac{1}{2} \, \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[-2\, \nabla_{\mu} \text{ tr} \left(\mu^T \Psi^{-1} \left(x^{(i)} - W z^{(i)}\right) \right) + \nabla_{\mu} \text{ tr} \left(\mu^T \Psi^{-1} \mu\right) \right]
$$

$\Psi$ is a symmetric matrix, therefore

$$
\Psi^{-T} = \Psi^{-1}
$$
</Info>

<Info>
$$
= -\frac{1}{2} \, \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[-2 \cdot \Psi^{-1} \left(x^{(i)} - W z^{(i)}\right) + 2 \cdot \Psi^{-1} \mu \right]
$$

For a symmetric matrix $A$

$$
\nabla_x \, x^T A x = 2Ax
$$
</Info>

$$
= \frac{1}{2} \, \Psi^{-1} \, \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[\left(x^{(i)} - W z^{(i)}\right) - \mu \right]
$$

Setting the derivative to zero and simplifying, we get:

$$
\sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[x^{(i)} - W z^{(i)}\right] = \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i}  \left[\mu\right]
$$

$$
\Rightarrow \sum_{i=1}^{n} \left(\mathbb{E}_{z^{(i)} \sim Q_i} \left[x^{(i)} \right] - W \cdot \mathbb{E}_{z^{(i)} \sim Q_i} \left[ z^{(i)}\right]\right) = n \cdot \mu
$$

Note that $\mu$ does not affect how each $z^{(i)}$ changes for each $x^{(i)}$. It simply shifts the mean of the distribution. Therefore, we can assume that for a large enough value of $n$, $\mathbb{E}_{z^{(i)} \sim Q_i} \left[z^{(i)}\right] = \mu_{z^{(i)}|x^{(i)}}$ is equal to the mean of the prior distribution which is $0$. Therefore, we get:

$$
\Rightarrow \sum_{i=1}^{n} \left(\mathbb{E}_{z^{(i)} \sim Q_i} \left[x^{(i)} \right] - W \cdot 0\right) = n \cdot \mu
$$

$$
\Rightarrow \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[x^{(i)} \right] = n \cdot \mu
$$

<Info>
$$
\Rightarrow \mu = \frac{1}{n} \sum_{i=1}^{n} x^{(i)}
$$

Expectation is over $z^{(i)}$, therefore:

$$
\mathbb{E} \left[ x^{(i)} \right] = x^{(i)}
$$
</Info>
</Derivation>

<Derivation className="mt-0 mb-0">
$$
\text{ELBO}\left(x; Q, \mu, W, \Psi \right) = \sum_{i=1}^n \mathbb{E}_{z^{(i)} \sim Q_i} \left[ \log \frac{p\left(x^{(i)}, z^{(i)}; \mu, W, \Psi\right)}{Q_i(z^{(i)})} \right]
$$

<Info>
$$
\approx \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[ -\frac{1}{2} \log |\Psi| - \frac{n}{2} \log(2\pi) - \frac{1}{2} (x^{(i)} - \mu - W z^{(i)})^T \Psi^{-1} (x^{(i)} - \mu - W z^{(i)}) \right]
$$

Dropping terms that do not depend on our parameters.
</Info>

<Lemma>
We won't prove this here but for any three matrices, the following lemma holds:

$$
\nabla_A \text{ tr}\left(ABA^TC\right) = CAB + C^TAB
$$
</Lemma>

Taking the derivative with respect to $W$, we get:

$$
-\frac{1}{2} \, \nabla_{W} \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[ \left(x^{(i)} - \mu - W z^{(i)}\right)^T \Psi^{-1} \left(x^{(i)} - \mu - W z^{(i)}\right) \right]
$$

<Info>
$$
= -\frac{1}{2} \, \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[-\nabla_{W} \left(z^{(i)T} W^T \Psi^{-1} \left(x^{(i)} - \mu \right)  \right) + \nabla_{W} \left(z^{(i)T} W^T \Psi^{-1} W z^{(i)} \right) - \nabla_{W} \left(\left(x^{(i)} - \mu\right)^T \Psi^{-1} W z^{(i)} \right) \right]
$$

Ignoring terms that do not depend on $W$.
</Info>

<Info>
$$
= -\frac{1}{2} \, \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[-\nabla_{W} \text{ tr} \left(z^{(i)T} W^T \Psi^{-1} \left(x^{(i)} - \mu \right)  \right) + \nabla_{W} \text{ tr} \left(z^{(i)T} W^T \Psi^{-1} W z^{(i)} \right) - \nabla_{W} \text{ tr} \left(\left(x^{(i)} - \mu\right)^T \Psi^{-1} W z^{(i)} \right) \right]
$$

All the three terms simplify to a scalar. And for a scalar $s$:

$$
\text{tr}(s) = s
$$
</Info>

<Info>
$$
= -\frac{1}{2} \, \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[-\nabla_{W} \text{ tr} \left(z^{(i)T} W^T \Psi^{-1} \left(x^{(i)} - \mu \right)  \right) + \nabla_{W} \text{ tr} \left(z^{(i)T} W^T \Psi^{-1} W z^{(i)} \right) - \nabla_{W} \text{ tr} \left(z^{(i)T} W^T \Psi^{-T} \left(x^{(i)} - \mu\right) \right) \right]
$$

$$
\text{tr}(AB) = \text{tr}(BA)
$$

$$
\text{tr}(A^T) = \text{tr}(A)
$$
</Info>

<Info>
$$
= -\frac{1}{2} \, \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[-2 \, \nabla_{W} \text{ tr} \left(z^{(i)T} W^T \Psi^{-1} \left(x^{(i)} - \mu \right)  \right) + \nabla_{W} \text{ tr} \left(z^{(i)T} W^T \Psi^{-1} W z^{(i)} \right) \right]
$$

$\Psi$ is a symmetric matrix, therefore

$$
\Psi^{-T} = \Psi^{-1}
$$
</Info>

<Info>
$$
= -\frac{1}{2} \, \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[-2 \, \nabla_{W} \text{ tr} \left(W^T \Psi^{-1} \left(x^{(i)} - \mu \right) z^{(i)T} \right) + \nabla_{W} \text{ tr} \left(W z^{(i)} z^{(i)T} W^T \Psi^{-1} \right) \right]
$$

Traces are cyclically permutable.

$$
\text{tr}(ABC) = \text{tr}(BCA) = \text{tr}(CAB)
$$
</Info>

<Info>
$$
= -\frac{1}{2} \, \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[-2 \, \Psi^{-1} \left(x^{(i)} - \mu \right) z^{(i)T} + \Psi^{-1} W z^{(i)} z^{(i)T} + \Psi^{-T} W z^{(i)} z^{(i)T} \right]
$$

$$
\nabla_A \text{ tr}\left(ABA^TC\right) = CAB + C^TAB
$$
</Info>

<Info>
$$
= \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[\, \Psi^{-1} \left(x^{(i)} - \mu \right) z^{(i)T} - \Psi^{-1} W z^{(i)} z^{(i)T} \right]
$$

$\Psi$ is a symmetric matrix, therefore

$$
\Psi^{-T} = \Psi^{-1}
$$
</Info>

Setting the derivative equal to zero and simplifying, we get:

$$
\Psi^{-1} \left(\sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[\left(x^{(i)} - \mu \right) z^{(i)T} \right] \right) = \Psi^{-1} W \left(\sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[z^{(i)} z^{(i)T} \right] \right)
$$

$$
\Rightarrow W = \left(\sum_{i=1}^{n} \left(x^{(i)} - \mu \right) \mathbb{E}_{z^{(i)} \sim Q_i} \left[z^{(i)T} \right] \right) \left(\sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[z^{(i)} z^{(i)T} \right] \right)^{-1}
$$

<Info>
$$
\Rightarrow W = \left(\sum_{i=1}^{n} \left(x^{(i)} - \mu \right) \mathbb{E}_{z^{(i)} \sim Q_i} \left[z^{(i)} \right]^T \right) \left(\sum_{i=1}^{n} \left(\mathbb{E}_{z^{(i)} \sim Q_i}\left[z^{(i)}\right] \cdot \mathbb{E}_{z^{(i)} \sim Q_i}\left[z^{(i)}\right]^T + \text{Cov}_{z^{(i)} \sim Q_i}\left(z^{(i)}, z^{(i)T}\right) \right) \right)^{-1}
$$

$$
\mathbb{E}(AB) = \mathbb{E}(A) \cdot \mathbb{E}(B) + \text{Cov}(A, B)
$$
</Info>

$$
W = \left( \sum_{i=1}^{n} (x^{(i)} - \mu) \mu_{z^{(i)}|x^{(i)}}^T \right) \left( \sum_{i=1}^{n} \mu_{z^{(i)}|x^{(i)}} \mu_{z^{(i)}|x^{(i)}}^T + \Sigma_{z^{(i)}|x^{(i)}} \right)^{-1}
$$
</Derivation>

<Derivation className="mt-0 mb-3">
$$
\text{ELBO}\left(x; Q, \mu, W, \Psi \right) = \sum_{i=1}^n \mathbb{E}_{z^{(i)} \sim Q_i} \left[ \log \frac{p\left(x^{(i)}, z^{(i)}; \mu, W, \Psi\right)}{Q_i(z^{(i)})} \right]
$$

<Info>
$$
\approx \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[ -\frac{1}{2} \log |\Psi| - \frac{n}{2} \log(2\pi) - \frac{1}{2} (x^{(i)} - \mu - W z^{(i)})^T \Psi^{-1} (x^{(i)} - \mu - W z^{(i)}) \right]
$$

Dropping terms that do not depend on our parameters.
</Info>

<Lemma>
We won't prove this here, but there are two Lemmas we need here:

$$
\text{1. } \nabla_A | A | = |A|(A^{-1})^T
$$

$$
\text{2. } \nabla_A \left(x^T A^{-1} x \right) = -A^{-1} x x^T A^{-1}
$$
</Lemma>

Taking the derivative with respect to $\Psi$, we get:

<Info>
$$
-\frac{1}{2} \, \nabla_{\Psi} \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i} \left[\log |\Psi| + \left(x^{(i)} - \mu - W z^{(i)}\right)^T \Psi^{-1} \left(x^{(i)} - \mu - W z^{(i)}\right) \right]
$$

Ignoring terms that do not depend on $\Psi$.
</Info>

<Info>
$$
= -\frac{1}{2} \, \sum_{i=1}^{n} \left(\mathbb{E}_{z^{(i)} \sim Q_i} \left[\frac{1}{|\Psi|} \cdot \nabla_{\Psi} |\Psi| \right] + \mathbb{E}_{z^{(i)} \sim Q_i}\left[-\Psi^{-1} \left(x^{(i)} - \mu - W z^{(i)}\right) \left(x^{(i)} - \mu - W z^{(i)}\right)^T \Psi^{-1} \right] \right)
$$

Using Lemma 2
</Info>

<Info>
$$
= -\frac{1}{2} \, \sum_{i=1}^{n} \left(\mathbb{E}_{z^{(i)} \sim Q_i} \left[\frac{1}{|\Psi|} \cdot |\Psi| \cdot \Psi^{-T} \right] + \mathbb{E}_{z^{(i)} \sim Q_i}\left[-\Psi^{-1} \left(x^{(i)} - \mu - W z^{(i)}\right) \left(x^{(i)} - \mu - W z^{(i)}\right)^T \Psi^{-1} \right] \right)
$$

Using Lemma 1
</Info>

<Info>
$$
= -\frac{1}{2} \, \sum_{i=1}^{n} \left(\mathbb{E}_{z^{(i)} \sim Q_i} \left[\Psi^{-1} \right] + \mathbb{E}_{z^{(i)} \sim Q_i}\left[-\Psi^{-1} \left(x^{(i)} - \mu - W z^{(i)}\right) \left(x^{(i)} - \mu - W z^{(i)}\right)^T \Psi^{-1} \right] \right)
$$

$\Psi^{-1}$ is a symmetric matrix and therefore $\Psi^{-T} = \Psi^{-1}$
</Info>

$$
= -\frac{1}{2} \, \sum_{i=1}^{n} \left(\Psi^{-1} - \Psi^{-1} \cdot \mathbb{E}_{z^{(i)} \sim Q_i}\left[ \left(x^{(i)} - \mu - W z^{(i)}\right) \left(x^{(i)} - \mu - W z^{(i)}\right)^T \right] \cdot \Psi^{-1} \right)
$$

Setting the derivative to zero and simplifying, we get:

$$
\sum_{i=1}^{n} \Psi^{-1} = \Psi^{-1} \cdot \left(\sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i}\left[ \left(x^{(i)} - \mu - W z^{(i)}\right) \left(x^{(i)} - \mu - W z^{(i)}\right)^T \right] \right) \cdot \Psi^{-1}
$$

$$
\Rightarrow n\cdot\Psi = \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i}\left[ \left(x^{(i)} - \mu - W z^{(i)}\right) \left(x^{(i)} - \mu - W z^{(i)}\right)^T \right]
$$

$$
\Rightarrow \Psi = \frac{1}{n} \, \sum_{i=1}^{n} \mathbb{E}_{z^{(i)} \sim Q_i}\left[ \left(x^{(i)} - \mu\right) \left(x^{(i)} - \mu\right)^T - W z^{(i)} \left(x^{(i)} - \mu\right)^T - \left(x^{(i)} - \mu\right) z^{(i)T} W^T + W z^{(i)} z^{(i)T} W^T \right]
$$

$$
\Rightarrow \Psi = \frac{1}{n} \, \sum_{i=1}^{n} \left(\left(x^{(i)} - \mu\right) \left(x^{(i)} - \mu\right)^T - W \mu_{z^{(i)} \mid x^{(i)}} \left(x^{(i)} - \mu\right)^T - \left(x^{(i)} - \mu\right) \mu_{z^{(i)} \mid x^{(i)}}^T W^T \right.
$$

<Info>
$$
\left. + \, W \left(\mathbb{E}_{z^{(i)} \sim Q_i}\left[z^{(i)}\right] \mathbb{E}_{z^{(i)} \sim Q_i}\left[z^{(i)}\right]^T + \text{Cov}_{z^{(i)} \sim Q_i}\left(z^{(i)}, z^{(i)T}\right)\right) W^T \right)
$$

$$
\mathbb{E}(AB) = \mathbb{E}(A) \mathbb{E}(B) + \text{Cov}(A, B)
$$
</Info>

$$
\Rightarrow \Psi = \frac{1}{n} \, \sum_{i=1}^{n} \left(\left(x^{(i)} - \mu\right) \left(x^{(i)} - \mu\right)^T - W \mu_{z^{(i)} \mid x^{(i)}} \left(x^{(i)} - \mu\right)^T - \left(x^{(i)} - \mu\right) \mu_{z^{(i)} \mid x^{(i)}}^T W^T \right.
$$

$$
\left. + \, W \left( \mu_{z^{(i)}|x^{(i)}} \, \mu_{z^{(i)}|x^{(i)}}^T + \Sigma_{z^{(i)}|x^{(i)}} \right) W^T \right)
$$
</Derivation>